// quando eu uso o export eu posso importar em qualquer lugar
// default dá um nome a funcao
export default function sum(a, b) {
  return a + b;
}

export function sub(a, b) {
  return a - b;
}

export function mult(a, b) {
  return a * b;
}
